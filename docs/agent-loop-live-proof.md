# Live Agent Loop proof

This file was created by the live governed workflow used by Mission Control. The OpenAI-compatible client surface is `/v1`; `/mcp` remains the MCP surface. The implementation step is valid only when the workflow report carries changed-path evidence.
