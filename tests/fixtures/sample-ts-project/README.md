# sample-ts-project

A minimal TypeScript fixture consumed by FolderForge integration tests.

It exists so the workspace detector reports `typescript`, and so search/code
tools have stable, known symbols (`Calculator`, `add`, `subtract`, `TAX_RATE`).
