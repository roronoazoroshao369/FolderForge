[project]
name = "sample-python-project"
version = "1.0.0"
description = "Fixture Python project used by FolderForge integration tests."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
