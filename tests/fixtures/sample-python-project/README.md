# sample-python-project

A minimal Python fixture consumed by FolderForge integration tests.

It exists so the workspace detector reports `python` / `pytest`, and so the
search and code tools have stable symbols (`greet`, `Greeter`, `DEFAULT_NAME`).
